import * as Local from "./styles";

const CardsTeachers = () => {
  return (
    <Local.Container>
      <Local.Content>{<Local.Icon></Local.Icon>}</Local.Content>
    </Local.Container>
  );
};

export default CardsTeachers;
